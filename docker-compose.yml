services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: device-management-db
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Admin1234!"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql

  #  api:
  #   build:
  #     context: .
  #     dockerfile: DeviceManagement.API/Dockerfile
  #   container_name: device-management-api
  #   ports:
  #     - "5000:8080"
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Development
  #     - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=DeviceManagementDB;User Id=sa;Password=YourPassword123!;TrustServerCertificate=True;
  #   depends_on:
  #     sqlserver:
  #       condition: service_healthy

  # frontend:
  #   build:
  #     context: ./device-management-ui
  #     dockerfile: Dockerfile
  #   container_name: device-management-ui
  #   ports:
  #     - "4200:80"
  #   depends_on:
  #     - api

volumes:
  sqlserver_data:
